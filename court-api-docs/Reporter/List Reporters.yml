info:
  name: List Reporters
  type: http

http:
  method: GET
  url: '{{api_url}}/api/reporters'
  auth: inherit

runtime:
  variables:
    - name: api_url
      value: http://localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
