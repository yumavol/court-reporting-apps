info:
  name: Patch Job
  type: http
  seq: 3

http:
  method: PATCH
  url: '{{api_url}}/api/jobs/:id'
  params:
    - name: id
      value: ''
      type: path
      enabled: true
  body:
    type: json
    data: |-
      {
        "durationMinutes": 90
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
