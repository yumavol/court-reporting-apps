info:
  name: Update Job Status
  type: http
  seq: 4

http:
  method: PATCH
  url: '{{api_url}}/api/jobs/:id/status'
  params:
    - name: id
      value: cmokc89ic0000jfz222ebtqzz
      type: path
  body:
    type: form-urlencoded
    data:
      - name: status
        value: NEW

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
