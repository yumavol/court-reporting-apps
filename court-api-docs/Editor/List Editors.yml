info:
  name: List Editors
  type: http

http:
  method: GET
  url: '{{api_url}}/api/editors'
  auth: inherit

runtime:
  variables:
    - name: api_url
      value: http://localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
